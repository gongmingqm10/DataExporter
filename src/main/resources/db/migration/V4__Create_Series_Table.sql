CREATE TABLE series (
    ID INTEGER not null PRIMARY KEY,
    code1 VARCHAR(100),
    launched_at DATETIME,
    amount1 DOUBLE,
    quantity1 INTEGER,
    quantity2 INTEGER,
    quantity3 INTEGER,
    quantity4 INTEGER,
    quantity5 INTEGER,
    quantity6 INTEGER,
    quantity7 INTEGER,
    quantity8 INTEGER
);